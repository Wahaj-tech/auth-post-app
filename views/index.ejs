<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full h-screen bg-zinc-900 p-5 text-white">
        <div class="w-full h-screen bg-zinc-900 p-5 text-white">

        <!-- Flash Messages -->
        <% if (success && success.length>0) { %>
            <div class="p-3 w-fit bg-green-600 text-white rounded mb-3">
                <%= success %>
            </div>
        <% } %>

        <% if (error && error.length>0) { %>
            <div class="p-3 w-fit bg-red-600 text-white rounded mb-3">
                <%= error %>
            </div>
        <% } %>
        <h1 class="text-3xl ">CREATE ACCOUNT</h1>
        <form action="/register" method="post">
            <input type="text" name="name"          class="px-3 py-2 outline-none bg-transparent rounded-md border-2 border-zinc-800" placeholder="name" >
            <input type="text" name="username"      class="px-3 py-2 outline-none bg-transparent rounded-md border-2 border-zinc-800" placeholder="username">
            <input type="password" name="password"  class="px-3 py-2 outline-none bg-transparent rounded-md border-2 border-zinc-800" placeholder="password" >
            <input type="email" name="email"        class="px-3 py-2 outline-none bg-transparent rounded-md border-2 border-zinc-800" placeholder="email">
            <input type="number" name="age"         class="px-3 py-2 outline-none bg-transparent rounded-md border-2 border-zinc-800" placeholder="age">
            <input type="submit" value="create account" class="p-2 bg-blue-500 rounded-lg">
        </form>
    </div>
</body>
</html>