<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full h-screen p-10 text-white bg-zinc-900">
        <h1 class="text-3xl ">LOGIN ACCOUNT</h1>
        <form action="/login" method="post">
            
            <input type="email" name="email"        class="px-3 py-2 outline-none bg-transparent rounded-md border-2 border-zinc-800" placeholder="email">

            <input type="password" name="password"  class="px-3 py-2 outline-none bg-transparent rounded-md border-2 border-zinc-800" placeholder="password" >

            <input type="submit" value="Login account" class="p-2 bg-blue-500 rounded-lg">
        </form>
        <% if (success && success.length>0) { %>
            <div class="p-3 w-100 bg-green-600 text-white rounded mb-3">
                <%= success %>
            </div>
        <% } %>

        <% if (error && error.length>0) { %>
            <div class="p-3 w-50 bg-red-600 text-white rounded mb-3">
                <%= error %>
            </div>
        <% } %>
    </div>
</body>
<script>
    // If token cookie exists, redirect to profile automatically
    if (sessionStorage.getItem("loggedIn") === "true") {
        window.location.href = "/profile";
    }
</script>

</html>